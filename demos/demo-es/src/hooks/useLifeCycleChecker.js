// import { useEffect } from 'riact';

const useLifeCycleChecker = function(name) {
  // console.log(`${name} rendering`);
  // useEffect(() => {
  //   console.log(`${name} mounted`);
  //   return () => {
  //     console.log(`${name} unmounted`);
  //   };
  // }, []);
};

export default useLifeCycleChecker;
